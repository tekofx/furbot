version: '3'
services:
  furbot:
    image: ghcr.io/tekofx/furbot
    container_name: furbot
    ports:
      - "100:80"

    volumes:
      - ./files:/bot/files/
      - ./databases:/bot/databases
      - ./env:/bot/env

    restart: unless-stopped
